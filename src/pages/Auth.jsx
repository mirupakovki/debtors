import React from 'react';

const Auth = () => {
    return (
        <div>
            <input type="text" />
            <input type="password" />
           <button>Войти</button>
        </div>
    );
};

export default Auth;